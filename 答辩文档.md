# ByteSavor 答辩文档

## 我们做了什么

一句话：**拍一张食材照片，AI 自动识别、推荐菜谱、生成购物清单，越用越懂你。**

## 整体思路

市面上有菜谱 App（下厨房）、有健康 App（薄荷健康）、有识图 App。但它们是**互相割裂的**——你要在 A 查菜谱、在 B 算热量、在 C 买菜，反复跳转。

我们把这些串成了一条线：

```
拍照识别食材 → 个性化推荐菜谱 → 合并购物清单 → 评分反馈学习
    (B感知)        (Y决策)          (T执行)        (E反馈)
```

这就是 **B-Y-T-E 智能闭环**。

## 核心创新点

### 创新一：多模态 VLM 识别 Skills 体系

我们不是简单地调个 API。围绕 Qwen-VL-Max 建了一套"技能"：

**1. 细粒度 Prompt——让 VLM 从"分类器"变"质检员"**

通用 VLM 只会说"这是西兰花"。我们的 prompt 要求返回：
- 食材名 + 置信度
- 新鲜度（高/中/低）
- 外观特征（颜色、纹理、饱满度）
- 分量估算（每个食材多少克）

相当于把 AI 训练成了食材质检员。

**2. 鲁棒解析器——VLM 输出什么格式都能接住**

VLM 返回不一定是标准 JSON。可能带 markdown 代码块、嵌在文字中间、甚至被截断。我们写了三级解析：
- 优先匹配 ```json 代码块
- 不行就从文本中间提取花括号
- 再不行返回空，打日志告警

配合 finish_reason 检测，如果 API 截断了输出会记录。

**3. 品质鉴定知识库——AI 只能看，我们让它能"判断"**

VLM 能看出颜色纹理，但不知道"好坏标准"。我们建了 50+ 种食材的挑选知识库：
- 西瓜 → 拍打听声清脆为佳，瓜脐小而凹
- 榴莲 → 浓郁香味为熟，果刺大而疏
- 西红柿 → 色泽鲜红均匀，果蒂翠绿

VLM 识别 + 知识库匹配 = 优/中/差品质报告。

**4. 三级降级链——保证演示不中断**

- 首选 DashScope 云 API（Qwen-VL-Max）
- 不可用 → Ollama 本地模型（qwen2.5-vl:7b）
- 还不行 → 提示用户"请重试或检查图片"

每一级失败都不报错，优雅降级。

**5. 前端图片压缩——3MB 变 200KB**

手机拍照原图 3-5MB，canvas 压缩到 800px 宽，上传速度从 15 秒降到 1-2 秒。

### 创新二：个性化推荐引擎

不是简单的"牛肉 → 推荐牛肉菜谱"。三层打分：

```
总分 = 食材匹配(50%) + 标签匹配(30%) + 偏好匹配(20%)
```

- **食材匹配**：你有的食材和菜谱需要的食材对比，精确+模糊匹配
- **标签匹配**：你的目标(减脂/增肌)匹配菜谱标签(高蛋白/低碳水)
- **偏好匹配**：你历史喜欢 spicy → spicy 菜谱加分

每条推荐带可解释理由："已有食材:牛肉" "高蛋白适合减脂" "符合偏好:辣味"

### 创新三：反馈学习闭环

你给菜谱打分 ≥4 星 → 菜谱的标签自动写入你的偏好。下次推荐 spicy 菜谱排前面。

不打分也行——去设置页手动选口味和健康目标，同样生效。

### 创新四：导出清单 + 选菜建议

选 3 道菜合并清单，同食材自动累加（牛肉 300g+400g=700g）。每项带挑选建议：
- 牛肉：纹理清晰、樱桃红色、大理石花纹均匀
- 西兰花：花球紧实、深绿色、无黄斑
- 虾仁：晶莹剔透、有自然弯曲弧度

支持人数调整（2人→4人自动翻倍）。

## 技术栈

| 层 | 技术 |
|----|------|
| 前端 | uni-app (Vue3 + Vite5)，H5 + 微信小程序 |
| 后端 | FastAPI + MySQL + Redis |
| AI | Qwen-VL-Max (视觉) + DeepSeek-Chat (推理) + Ollama (本地备份) |
| 数据 | Ta-da 数据集 2576 道菜谱 |
| 部署 | Docker Compose 一键启动 |

## 软件规模

| 指标 | 数值 |
|------|------|
| 核心接口 | 11 个 |
| 源文件 | 79 个 |
| 代码行数 | ~146k（含菜谱数据） |
| 自动化测试 | 23 个 pytest |

## 五个演示场景

| 场景 | 做什么 | 亮点 |
|------|--------|------|
| 拍照推荐 | 拍食材→识别→推荐→评分 | VLM 真 AI 识别 |
| 清单导出 | 3 道菜合并清单+选菜建议 | 同食材累加+知识库 |
| 品质鉴定 | 拍水果→优/中/差+挑选标准 | VLM + 知识库 |
| 营养分析 | 拍一顿饭→热量+份量参照 | 一拳=150g=175kcal |
| 探店向导 | 拍菜品→菜系+故事+吃法 | 8 道经典菜知识库 |

## 总结

我们不是做一个菜谱 App。我们是把**识别、推荐、执行、反馈**串成一条可追踪的 Agent 流水线。
