# ByteSavor V3.0 后端技术报告

> 答辩用文档。用通俗语言讲清楚：做了什么、怎么做的、为什么这么做。

---

## 一、项目是什么

ByteSavor 是一个 AI 饮食助手。用户拍一张冰箱照片，或者打字说"家里有牛肉和南瓜，30分钟做个减脂餐"，系统自动识别食材、推荐菜谱、生成购物清单，长期使用还会学用户的口味偏好。

技术上的核心是一条 **B-Y-T-E 智能闭环**：

```
B (Perception / 感知) → 看懂图片里有什么食材
Y (Decision / 决策)   → 根据食材和偏好推荐菜谱
T (Task / 执行)       → 生成购物清单
E (Feedback / 反馈)   → 收集评价，越用越懂你
```

这四个环节通过 Agent 编排层串联，形成完整的"从看到做"流水线。

---

## 二、技术选型与理由

| 层面 | 选型 | 理由 |
|------|------|------|
| API 框架 | FastAPI | Python 生态最强的异步框架，自带 Swagger 文档，跟 AI 库天然兼容 |
| 数据库 | MySQL 8.0 | 团队都用过，学习成本零；JSON 类型能存菜谱的非结构化数据 |
| 异步 | SQLAlchemy async + asyncmy | 真异步，AI 推理时不阻塞其他请求 |
| 认证 | JWT | 小程序场景，无状态 token 比 session 更合适 |
| AI 推理 | Ollama + OpenAI 兼容接口 | 本地免费跑，接口标准，后期切云端模型不用改代码 |
| 缓存 | Redis | 同一个"鸡蛋+减脂"查询可能一天来几十次，缓存 10 分钟省掉重算 |

选型原则：**用成熟的开源工具，不做重复造轮子。团队会上手的优先，不是最新最酷的就一定好。**

---

## 三、系统架构

### 3.1 分层结构

```
用户界面层 (uni-app 小程序)
    │  HTTPS
    ▼
业务逻辑层 (FastAPI)
  ├─ 路由层: 接收 HTTP 请求，参数校验
  ├─ 服务层: 真正的业务逻辑（推荐算法、购物合并、Agent 编排）
  ├─ 模型层: 数据库结构定义
  └─ 中间件: 认证拦截
    │
    ▼
基础服务层
  ├─ MySQL (用户数据、菜谱库)
  ├─ Redis (缓存)
  └─ Ollama (本地 LLM 推理)
```

每一层只做自己该做的事：路由不写业务逻辑，服务不直接操作 HTTP，模型只定义数据结构。这样改一层不影响其他层。

### 3.2 B-Y-T-E 数据流

以"用户打字说家里有牛肉南瓜、30分钟减脂"为例：

```
用户输入 → Agent 编排层
  │
  ├─ 意图解析 (LLM 或正则降级)
  │     "减脂" → goal=fat_loss
  │     "30分钟" → time_limit=30
  │     "牛肉、南瓜" → ingredients=["牛肉","南瓜"]
  │
  ├─ B阶段-感知
  │     如果没有图片就跳过；有图片调 VLM 识别食材
  │
  ├─ Y阶段-决策
  │     调推荐引擎：从 2573 道菜谱中匹配
  │     硬过滤（烹饪时间超过30分钟的直接淘汰）
  │     软排序（食材匹配度 + 标签匹配 + 偏好加权）
  │     → 返回 top 5 菜谱 + 营养缺口分析
  │
  ├─ T阶段-执行
  │     选中的菜谱合并购物清单（同名食材数量累加）
  │
  └─ 返回完整结果给前端
```

### 3.3 为什么不用更酷的技术

- **没用 Neo4j 图数据库**：2千多道菜谱，MySQL 的 JSON 字段完全够用。图数据库能查"鸡胸肉→高蛋白→适合减脂"这样的关系链，但 2573 条数据建图的价值不大。代码里留了 Neo4j 连接器接口，数据量上来随时切。
- **没用 LangGraph 状态机**：Agent 目前是顺序调用，不是真正的状态机。原因是 LangGraph 需要更强的 LLM（能自己做决策判断"再拍一次"还是"直接问用户"），我们本地的 Qwen 1.5B 做不了这个。代码里留了适配层，LLM 能力到位就能切。
- **没用 Docker Swarm/K8s**：团队规模不需要。单机 Docker Compose 一键启动已经够组员用了。

---

## 四、推荐引擎设计（核心模块）

这是整个项目最复杂的东西，单独拎出来讲。

### 4.1 问题

"家里有牛肉和西兰花" → 从 2573 道菜谱中找出最合适的 5 道。

### 4.2 五步 Pipeline

```
1. 候选检索: 从数据库查出所有菜谱
       ↓
2. 硬过滤:   cooking_time > time_limit → 淘汰
       ↓
3. 软排序:   食材匹配(50%) + 标签匹配(30%) + 偏好匹配(20%)
       ↓
4. Fallback: 如果全部被淘汰（time_limit=5分钟，没有菜能满足）
              → 放宽条件，降权返回最接近的
       ↓
5. 解释生成: 每条推荐带上"为什么"的理由
```

### 4.3 打分公式

```
总分 = 食材匹配分数 × 0.5 + 标签匹配分数 × 0.3 + 偏好匹配分数 × 0.2

食材匹配: 用户有的食材 ∩ 菜谱需要的食材 / 菜谱需要的食材数
  例: 用户有[牛肉,西兰花] + 菜谱需[牛肉,西兰花,蒜] → 2/3 = 0.67

标签匹配: 菜谱标签跟用户目标的重叠度
  用户选"减脂" + 菜谱标签有"high_protein,low_carb" → 加分

偏好匹配: 菜谱标签跟用户历史偏好的重叠度
  用户喜欢"spicy" + 菜谱标签有"spicy" → 加分
```

三个分都归一化到 0~1 之间，保证权重有效。

### 4.4 降级设计

- 探索模式：用户没传食材 → 全量推荐，只按标签和偏好排序
- Fallback：硬过滤全淘汰 → 放宽条件，降权标记为 NEAR_FIT
- 偏好缺失：没登录或新用户 → 偏好分给默认值 0.5，不影响排序
- VLM 不可用 → 降级返回 Mock 食材数据

### 4.5 可解释性

每条推荐都带着理由，不是光一个数字：

```json
{
  "title": "香辣牛肉西兰花",
  "match_score": 0.65,
  "reasons": [
    {"code": "ING_MATCH", "text": "已有食材: 牛肉"},
    {"code": "TASTE_MATCH", "text": "口味匹配: spicy"},
    {"code": "HIGH_PROTEIN_GOAL", "text": "高蛋白适合减脂"}
  ]
}
```

前端按 code 渲染对应文案，多语言直接换模板就行。

---

## 五、Agent 编排设计

### 5.1 设计目标

Agent 是 BYTE 四条管道的总调度。用户说一句话，Agent 自动决定：要调哪几个模块、按什么顺序、失败了怎么办。

### 5.2 当前实现

用 Provider 模式解耦。Agent 不直接 import 具体模块，而是接受三个函数作为参数：

```python
async def execute(user_input, sense_fn=None, decide_fn=None, task_fn=None):
```

router 在注入时用闭包把数据库会话绑进去。这样 Agent 不关心底层怎么实现的，只关心"给我一个函数，我能调它"。

### 5.3 每次调用的返回

不是只返回最终结果，而是每个阶段的执行状态都记录下来：

```json
{
  "trace_id": "abc123",
  "stages": [
    {"stage": "sense",     "status": "skipped", "latency_ms": 0},
    {"stage": "decision",  "status": "success", "latency_ms": 5},
    {"stage": "task",      "status": "success", "latency_ms": 3}
  ],
  "parsed_intent": {"goal": "fat_loss", "time_limit": 30, ...},
  "recipes": [...],
  "shopping_list": [...]
}
```

这样做有两个好处：前端能按阶段展示进度条（"正在识别食材..."→"正在推荐菜谱..."）；出了 bug 能马上定位是哪个阶段炸的。

### 5.4 降级链

意图解析：LLM 优先 → 超时/不可用 → 正则降级
食材识别：VLM 优先 → 不可用（没配置/超时/返回异常）→ Mock 降级
推荐：正常匹配 → 全淘汰 → Fallback 降权推荐

每一环都有兜底，不会因为 AI 挂了导致整个系统不可用。

---

## 六、用户系统

### 6.1 认证方案

用的是微信小程序的 OpenID 认证。流程：用户在小程序里授权 → 拿到微信 OpenID → 调后端注册/登录接口 → 后端返回 JWT token → 小程序存本地，以后每次请求带在 Header 里。

### 6.2 为什么不用密码

小程序场景下，用户不需要记密码。微信授权已经验证了身份，OpenID 就是这个用户的唯一标识。注册和登录其实是一个接口——第一次调用就是注册，后续就是静默登录。

### 6.3 数据表设计

三张表分开存，不把所有字段塞一起：

- users：基础身份（openid、昵称、手机号）
- profiles：用户画像（减脂/增肌目标、口味偏好、健康评分）
- nutrition_logs：营养记录（每天的营养分数、缺乏什么）

这样拆是因为营养记录是时间序列数据（每天一条），不能放 users 表里。profiles 拆出来是因为后面可能会加更多画像维度。

---

## 七、购物清单合并算法

### 7.1 问题

用户同时选了 3 道菜谱，每道都需要牛肉：
- 香辣牛肉西兰花：牛肉 300g
- 南瓜炖牛肉：牛肉 400g
- 牛肉面：牛肉 300g

合并后应该是"牛肉 1000g"，不是重复三次。

### 7.2 解法

1. 解析数量字符串："300g" → value=300, unit=g；"2个" → value=2, unit=个；"少许" → value=None, unit=少许
2. 同名食材检查单位是否兼容：都是 g 就可以累加，300g 和 2 个不能混
3. 不兼容单位的情况：保留为独立条目，用 amounts 数组表示

---

## 八、反馈与偏好学习

用户对菜谱打分后，系统会自动调偏好：

- 高分（≥ 4）：取这个菜谱的标签（如 spicy、high_protein），追加到用户偏好里
- 低分（≤ 2）：从这个菜谱的标签中，把匹配的从用户偏好里移除

这样用户越用，推荐越准——不需要手动设置口味。

---

## 九、数据迭代

- 第一版：手写 10 道菜谱，验证推荐 Pipeline 能跑通
- 第二版：扩展到 60 道，覆盖更多食材和口味，手工编写确保质量
- 第三版：引入 Ta-da Recipe Dataset（公开数据集），解析后 2537 道导入，同时保留源字段 `source` 标记区分。后期可以针对 AI 生成的菜谱加 `source="ai_gen"` 做质量控制

---

## 十、测试策略

- 白盒：对登录和打分两个核心方法画流图、算环形复杂度、找独立路径
- 黑盒：对注册和推荐两个功能做等价类划分（有效类/无效类/边界值）
- 自动化：pytest 写了 20 个测试用例，覆盖 agent 解析、shopping 合并、auth 认证、decision 推荐的正常/异常路径
- 全链路：前后端一起测了三个真实用户场景（注册拍照推荐、打字推荐反馈、网络异常）

---

## 十一、工程规范

1. 三层解耦：路由层不写 SQL，服务层不读 HTTP 请求，模型层只定义表结构
2. 异常兜底：全局 `@app.exception_handler` 统一返回 `{status:"error", error:{code, message}, trace_id}`
3. 事务保护：所有写操作带 try/except/rollback
4. 配置与代码分离：`.env` 管配置，`config.py` 读配置，代码不硬编码密码和地址
5. 接口契约不变：Schema 可以加字段，但路由路径和已有字段不删不改

---

## 十二、后续方向

| 优先级 | 事项 | 说明 |
|--------|------|------|
| P0 | 接真实 VLM | 代码就绪，等模型 |
| P1 | LangGraph 状态机 | 代码留了适配层，LLM 能力到位就切 |
| P2 | Neo4j 知识图谱 | 数据量 < 1万没必要，代码留了连接器 |
| P2 | 推荐模型升级 | 当前启发式打分，数据够多后可以训练 learning-to-rank |
