# 修改文档 19：v2 审查修复记录

## 日期
2026-06-20

## 修复清单

### P0-1 ✅ 弯引号修复
- 文件：`bsapp/src/pages/home/home.vue` 198-213 行
- 修改：Vue attributes 中 `"` `"` → `"`（ASCII）
- 检查：`python3 -c` Unicode scan 无 U+201C/201D/2018/2019
- 构建：`npm run build:h5` — DONE Build complete

### P0-2 ✅ 构建通过
- 同上

### P1-1 ✅ MemoryContext previous_state
- 文件：`app/agent/langgraph_runtime.py` — 新增 `get_previous_state()` 公开方法
- 文件：`app/routers/agent.py` — 前置调用 `runtime.get_previous_state()`，传入 `build_memory_context()`
- 检查：47 测试通过

### P1-2 ✅ _result() 返回 memory 字段
- 文件：`app/agent/runtime.py` — `_result()` 新增 `memory_context`、`memory_used` 字段
- 文件：`app/routers/agent.py` — `result.get("memory_context") or memory_context` 优先取 state 值

### P1-3 ✅ 统一事件顺序
- 文件：`app/agent/runtime.py` — `_finish_run()` 先 evaluation 再 final
- 顺序：ROUTING → EXECUTING → EVALUATING → FINISHED

### P1-4 ✅ reply 只从 final event 取
- 文件：`app/agent/runtime.py` — `_result()` 用 `event.get("type") == "final"` 过滤
- fallback：无 final event 时调 `AgentRuntime._build_reply(state)`

### P1-5 ✅ confirmation_prompts 按 issue code 独立生成
- 文件：`app/routers/agent.py` — 收集所有 evaluation codes，按 code 独立 append
- 去重：`seen_codes` set 防重复

### P1-6 ✅ evaluator 不把 PASS 降为 PARTIAL
- 文件：`app/agent/evaluator.py` — NEEDS_USER_CONFIRMATION 仅追加 issue，不改变 verdict
- 更新测试：`test_evaluator_needs_user_confirmation` 期望 PASS

### P1-7 ✅ 核心食材用 intent.ingredients
- 文件：`app/agent/evaluator.py` — `core_ingredients = state["intent"].get("ingredients") or []`
- 不再用全部 state["ingredients"]（可能含VLM识别的大量食材）

### P1-8 ✅ 推荐/evaluator 复用同义词
- 文件：`app/services/decision.py` — `_normalize_ingredient_name()` 调用 `normalize_synonym()`
- 文件：`app/agent/evaluator.py` — `_count_core_ingredient_coverage()` 使用 `normalize_ingredient_name()`

### P1-9 ✅ 前端识别页调用 correction_logs
- 文件：`bsapp/src/api/index.js` — 新增 `recordCorrection(payload)`
- 文件：`bsapp/src/pages/ingredient-recognition/ingredient-recognition.vue` — `editIngredient`/`deleteIngredient` 调用 correction API

### P1-10 ✅ 注册昵称写入后端
- 文件：`app/schemas.py` — `RegisterRequest` 新增 `name: str = ""`
- 文件：`app/routers/auth.py` — `create_user(db, req.openid, name=req.name)`
- 文件：`bsapp/src/api/index.js` — `register(openid, name)`
- 文件：`bsapp/src/pages/register/register.vue` — 传 `nickname.value.trim()`

### P1-12 ✅ handleConfirmation retry 使用原始输入
- 文件：`bsapp/src/pages/home/home.vue` — assistant 消息存 `sourceText`，retry 时用它而非助手回复

### P1-14 ✅ 前后端营养数据对齐
- 文件：`bsapp/src/utils/food-analysis.js` — 鸡蛋/牛肉/鸡肉/虾/鱼/南瓜/西瓜/番茄等对齐后端 `nutrition_calculator.py`

### P1-15 ✅ correction_logs Pydantic 校验
- 文件：`app/schemas.py` — 新增 `CorrectionLogRequest` 模型，action/source 使用 pattern 约束
- 文件：`app/routers/correction_logs.py` — 参数改为 `body: CorrectionLogRequest`

### P2-2 ✅ 文档同步
- 15/16/18 已复制到 `！！！ByteSavor文档_打开这里！！！/迭代修改_2026-06-19/`

### P2-3 ✅ PPT 恢复
- PPT 存在于 `docs/ByteSavor.pptx` 和 `期末展演/`，已恢复到 `bsapp/ByteSavor.pptx`

## 未修复（说明原因）

| 编号 | 问题 | 原因 |
|------|------|------|
| P1-11 | 密码注册 | 演示版使用 OpenID 直传，已在 `auth.py:9-11` 注明安全说明 |
| P1-13 | replayingEvents 状态 | 当前设计是 HTTP 返回后立即显示回复+events 逐条回放，14 文档已注明此行为 |
| P1-16 | VLM 缓存 cache_hit | 短期无风险，cache_get 每次 JSON loads 不污染 |
| P1-17 | _events_to_stages phase | stages 仅作旧版兼容，完整时间线用 events |
| P2-1 | README 范围 | 此处文档同步，git 范围见 git status |
| P2-4 | MySQL 测试 | 当前沙箱无法连接，需真实环境运行 |

## 测试结果
- 前端 H5 构建：通过
- 核心 Agent 非 DB 测试：58 passed
- 宽范围测试：当前沙箱 77 passed / 5 failed（失败在 test_decision.py，MySQL 连接被沙箱拒绝）
- DB 依赖测试需 MySQL+Redis 环境复核
